# Fixture generation scripts

Scripts that produce synthetic fixtures on demand (for tests that need too
many files to reasonably check in).

- `generate_large_repo.py` — populates `large_repo_shallow/` with N trivial
  Python files for shallow-mode tests.
